# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# Leave MONGODB_URI empty to run in Fallback Mode (JSON database)
MONGODB_URI=

# Authentication
# Change this to a secure key in production
JWT_SECRET=super_secret_jwt_key_12345678
ADMIN_PASSWORD=admin123
